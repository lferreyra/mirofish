## AI / Robotics / Lasers Scan v5

This is the second fresh live scan batch run through the current v4 engine.

Universe:

- `AXTI`
- `MTSI`
- `OUST`
- `AMBA`
- `LASR`
- `TER`

### Ranked output

1. `AXTI` -> `shares` (`75.32`)
2. `MTSI` -> `shares` (`69.34`)
3. `LASR` -> `shares` (`68.59`)
4. `OUST` -> `shares` (`68.07`)
5. `AMBA` -> `shares` (`67.13`)
6. `TER` -> `shares` (`62.64`)

### Read

This batch did not surface a new `LEAPS` pick.

That is not necessarily a failure. The engine is being conservative because this batch lacks the richer chain and resale evidence we already had for `MP`.

The most interesting result is `AXTI`:

- highest mispricing score in the batch
- strongest hidden-upstream bottleneck profile
- still not enough LEAPS bias to beat shares under current assumptions

The rest look like equity ideas first:

- `MTSI`: cleaner public-market expression than the smaller optical names
- `LASR`: plausible hidden defense-laser equity
- `OUST`: nonlinear robotics/autonomy equity, but still too thin for a strong LEAPS case
- `AMBA`: decent edge-AI robotics equity, but not especially asymmetric on the current lens
- `TER`: mostly a stock-quality / optional-segment story, not a vol-mispricing story

### Main takeaway

The second batch supports the new diagnosis:

- the engine can now separate stock vs LEAPS better
- but it only promotes `LEAPS` when we have strong option-specific evidence

That means the next way to get more `LEAPS` picks is not more bottleneck tuning alone. It is either:

- richer chain evidence for candidate names like `AXTI`
- or better heuristics for when small-cap asymmetry should imply stale long-dated optionality even before direct chain capture
