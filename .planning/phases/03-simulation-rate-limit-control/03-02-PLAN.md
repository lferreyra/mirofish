---
phase: 03-simulation-rate-limit-control
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/api/simulation.js
  - frontend/src/components/Step3Simulation.vue
autonomous: false
requirements: [R3.3]

must_haves:
  truths:
    - "User can see and adjust rate limit settings before starting a simulation"
    - "Settings are persisted across page reloads via localStorage"
    - "Settings are sent to the backend before simulation starts"
    - "Settings panel is hidden once simulation is running"
  artifacts:
    - path: "frontend/src/api/simulation.js"
      provides: "updateSimulationConfig API helper"
      contains: "updateSimulationConfig"
    - path: "frontend/src/components/Step3Simulation.vue"
      provides: "Collapsible rate limit settings panel"
      contains: "rate-limit-settings"
  key_links:
    - from: "frontend/src/components/Step3Simulation.vue"
      to: "frontend/src/api/simulation.js"
      via: "import { updateSimulationConfig }"
      pattern: "updateSimulationConfig"
    - from: "frontend/src/components/Step3Simulation.vue"
      to: "localStorage"
      via: "watch() saves on any change"
      pattern: "mirofish_rate_limit_settings"
    - from: "frontend/src/components/Step3Simulation.vue"
      to: "POST /api/simulation/{id}/config"
      via: "updateSimulationConfig called before startSimulation"
      pattern: "updateSimulationConfig.*simulationId"
---

<objective>
Add a collapsible rate limit settings panel to the simulation view with localStorage persistence, and wire it to the backend config endpoint before simulation start.

Purpose: Allow users to configure inter-turn delay, retry behavior, and TPM/RPM limits before running a simulation.
Output: Settings UI in Step3Simulation.vue, API helper in simulation.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simulation-rate-limit-control/03-CONTEXT.md
@.planning/phases/03-simulation-rate-limit-control/03-RESEARCH.md
@.planning/phases/03-simulation-rate-limit-control/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: the POST endpoint this plan must call -->
POST /api/simulation/{id}/config
  Body: { "rate_limit": { "inter_turn_delay_ms": 500, "max_retries": 3, "retry_base_delay_s": 30, "tpm_limit": 0, "rpm_limit": 0 } }
  Response: { "success": true, "data": { "rate_limit": {...} } }

<!-- Existing API helper pattern from simulation.js -->
From frontend/src/api/simulation.js:
```javascript
import service, { requestWithRetry } from './index'
// Pattern: export const fnName = (params) => { return service.post(url, data) }
```

<!-- Step3Simulation.vue key state -->
From frontend/src/components/Step3Simulation.vue:
```javascript
const phase = ref(0)  // 0: not started, 1: running, 2: completed
const props = defineProps({ simulationId: String, maxRounds: Number, ... })
const doStartSimulation = async () => { /* starts at line 380 */ }
// startSimulation is called at line 409: const res = await startSimulation(params)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API helper and settings panel with localStorage persistence</name>
  <files>frontend/src/api/simulation.js, frontend/src/components/Step3Simulation.vue</files>
  <read_first>
    - frontend/src/api/simulation.js (full file — existing API pattern)
    - frontend/src/components/Step3Simulation.vue (full file — understand template structure, script setup, existing styles)
    - .planning/phases/03-simulation-rate-limit-control/03-CONTEXT.md (decisions D-07 through D-10)
  </read_first>
  <action>
**A. Add `updateSimulationConfig` to `frontend/src/api/simulation.js`** (per D-10):

Add at end of file, before any default export if present:
```javascript
/**
 * Update simulation rate limit config
 * @param {string} simulationId
 * @param {Object} data - { rate_limit: { inter_turn_delay_ms, max_retries, retry_base_delay_s, tpm_limit, rpm_limit } }
 */
export const updateSimulationConfig = (simulationId, data) => {
  return service.post(`/api/simulation/${simulationId}/config`, data)
}
```

**B. Add settings panel to `Step3Simulation.vue`** (per D-07, D-08, D-09, D-10):

1. In the `<script setup>` imports section, add `updateSimulationConfig` to the import from `'../api/simulation'`:
   ```javascript
   import {
     startSimulation,
     stopSimulation,
     // ... existing imports ...
     updateSimulationConfig
   } from '../api/simulation'
   ```

   Also ensure `watch` and `onMounted` are imported from 'vue' (they likely already are — check first).

2. Add reactive state refs AFTER the existing state declarations (after line ~324):
   ```javascript
   // Rate limit settings
   const showRateLimitSettings = ref(false)
   const rateLimitSettings = ref({
     inter_turn_delay_ms: 500,
     max_retries: 3,
     retry_base_delay_s: 30,
     tpm_limit: 0,
     rpm_limit: 0
   })
   const RATE_LIMIT_STORAGE_KEY = 'mirofish_rate_limit_settings'
   ```

3. Add localStorage load in onMounted (find existing onMounted if any, or add new one):
   ```javascript
   onMounted(() => {
     const saved = localStorage.getItem(RATE_LIMIT_STORAGE_KEY)
     if (saved) {
       try {
         Object.assign(rateLimitSettings.value, JSON.parse(saved))
       } catch (_) { /* ignore malformed data */ }
     }
   })
   ```
   If an `onMounted` already exists, merge the localStorage load into it.

4. Add watch for auto-save:
   ```javascript
   watch(rateLimitSettings, (val) => {
     localStorage.setItem(RATE_LIMIT_STORAGE_KEY, JSON.stringify(val))
   }, { deep: true })
   ```

5. In `doStartSimulation()` (around line 380), BEFORE `const res = await startSimulation(params)` (line 409), add:
   ```javascript
   // Send rate limit settings to backend before starting
   try {
     await updateSimulationConfig(props.simulationId, {
       rate_limit: rateLimitSettings.value
     })
     addLog('Rate limit settings applied')
   } catch (configErr) {
     addLog(`Warning: Failed to apply rate limit settings: ${configErr.message || configErr}`)
     // Non-blocking — simulation can still start with defaults
   }
   ```

6. In the `<template>`, add the collapsible settings panel. Insert it INSIDE the `.simulation-panel` div, BEFORE the main content that shows the timeline/actions. Place it so it is only visible when `phase === 0` (not started). Find a suitable location early in the template (after the control-bar div). Add:

   ```html
   <!-- Rate Limit Settings (pre-run only) -->
   <div v-if="phase === 0" class="rate-limit-settings">
     <div class="settings-toggle" @click="showRateLimitSettings = !showRateLimitSettings">
       <span class="settings-toggle-label">Rate Limit Settings</span>
       <span class="toggle-arrow">{{ showRateLimitSettings ? '\u25B2' : '\u25BC' }}</span>
     </div>
     <div v-if="showRateLimitSettings" class="settings-body">
       <div class="setting-row">
         <label class="setting-label">Inter-turn Delay</label>
         <div class="setting-control">
           <input type="range" v-model.number="rateLimitSettings.inter_turn_delay_ms"
                  min="0" max="5000" step="100" class="setting-slider" />
           <span class="setting-value mono">{{ rateLimitSettings.inter_turn_delay_ms }}ms</span>
         </div>
       </div>
       <div class="setting-row">
         <label class="setting-label">Max Retries</label>
         <div class="setting-control">
           <input type="number" v-model.number="rateLimitSettings.max_retries"
                  min="1" max="10" class="setting-input" />
         </div>
       </div>
       <div class="setting-row">
         <label class="setting-label">Retry Base Delay (s)</label>
         <div class="setting-control">
           <input type="number" v-model.number="rateLimitSettings.retry_base_delay_s"
                  min="1" max="120" class="setting-input" />
         </div>
       </div>
       <div class="setting-row">
         <label class="setting-label">TPM Limit <span class="setting-hint">(0 = unlimited)</span></label>
         <div class="setting-control">
           <input type="number" v-model.number="rateLimitSettings.tpm_limit"
                  min="0" step="1000" class="setting-input" />
         </div>
       </div>
       <div class="setting-row">
         <label class="setting-label">RPM Limit <span class="setting-hint">(0 = unlimited)</span></label>
         <div class="setting-control">
           <input type="number" v-model.number="rateLimitSettings.rpm_limit"
                  min="0" class="setting-input" />
         </div>
       </div>
     </div>
   </div>
   ```

7. In the `<style scoped>` section, add styles for the settings panel. Use existing CSS custom properties (var(--card), var(--border), var(--foreground), var(--muted-foreground), var(--primary), var(--accent)):

   ```css
   .rate-limit-settings {
     margin: 12px 16px;
     border: 1px solid var(--border);
     border-radius: 8px;
     background: var(--card);
     overflow: hidden;
   }
   .settings-toggle {
     display: flex;
     justify-content: space-between;
     align-items: center;
     padding: 10px 16px;
     cursor: pointer;
     user-select: none;
     color: var(--foreground);
     font-size: 13px;
     font-weight: 500;
   }
   .settings-toggle:hover {
     background: var(--secondary);
   }
   .toggle-arrow {
     font-size: 10px;
     color: var(--muted-foreground);
   }
   .settings-body {
     padding: 8px 16px 16px;
     border-top: 1px solid var(--border);
   }
   .setting-row {
     display: flex;
     justify-content: space-between;
     align-items: center;
     padding: 8px 0;
   }
   .setting-label {
     font-size: 12px;
     color: var(--muted-foreground);
     min-width: 140px;
   }
   .setting-hint {
     font-size: 10px;
     opacity: 0.6;
   }
   .setting-control {
     display: flex;
     align-items: center;
     gap: 8px;
   }
   .setting-slider {
     width: 140px;
     accent-color: var(--primary);
   }
   .setting-value {
     font-size: 12px;
     color: var(--foreground);
     min-width: 60px;
     text-align: right;
   }
   .setting-input {
     width: 80px;
     padding: 4px 8px;
     border: 1px solid var(--border);
     border-radius: 4px;
     background: var(--background);
     color: var(--foreground);
     font-size: 12px;
     text-align: right;
   }
   .setting-input:focus {
     outline: none;
     border-color: var(--primary);
   }
   ```

IMPORTANT: The settings panel must ONLY appear when `phase === 0`. Once the simulation starts (`phase === 1`), the panel is hidden. This prevents config file writes during a running simulation (Pitfall 2 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd "C:/Users/lucas/OneDrive/Documentos/AI/VS Code/MiroFish SIPE/mirofish" && grep -c "updateSimulationConfig" frontend/src/api/simulation.js frontend/src/components/Step3Simulation.vue && grep -c "mirofish_rate_limit_settings" frontend/src/components/Step3Simulation.vue && grep -c "rate-limit-settings" frontend/src/components/Step3Simulation.vue</automated>
  </verify>
  <acceptance_criteria>
    - `simulation.js` contains `export const updateSimulationConfig = (simulationId, data) =>` with `service.post(\`/api/simulation/${simulationId}/config\`, data)`
    - `Step3Simulation.vue` imports `updateSimulationConfig` from `'../api/simulation'`
    - `Step3Simulation.vue` contains `const rateLimitSettings = ref({` with all 5 fields: `inter_turn_delay_ms: 500`, `max_retries: 3`, `retry_base_delay_s: 30`, `tpm_limit: 0`, `rpm_limit: 0`
    - `Step3Simulation.vue` contains `const RATE_LIMIT_STORAGE_KEY = 'mirofish_rate_limit_settings'`
    - `Step3Simulation.vue` contains `localStorage.getItem(RATE_LIMIT_STORAGE_KEY)` in onMounted
    - `Step3Simulation.vue` contains `watch(rateLimitSettings,` with `{ deep: true }`
    - `Step3Simulation.vue` contains `localStorage.setItem(RATE_LIMIT_STORAGE_KEY,`
    - `Step3Simulation.vue` contains `await updateSimulationConfig(props.simulationId,` BEFORE `await startSimulation(params)`
    - `Step3Simulation.vue` template contains `v-if="phase === 0"` on the `.rate-limit-settings` div
    - `Step3Simulation.vue` template contains `class="rate-limit-settings"` and `class="settings-toggle"`
    - `Step3Simulation.vue` template contains slider input with `min="0" max="5000" step="100"`
    - `Step3Simulation.vue` style section contains `.rate-limit-settings` and `.settings-body` and `.setting-slider`
  </acceptance_criteria>
  <done>Collapsible rate limit settings panel visible pre-run in Step3Simulation.vue. All 5 controls present (delay slider, max retries, retry base delay, TPM limit, RPM limit). Settings load from and save to localStorage. Settings POSTed to backend before simulation start.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of rate limit settings panel</name>
  <files>frontend/src/components/Step3Simulation.vue</files>
  <action>Human verifies the settings panel renders correctly, persists settings, and fires the config POST before simulation start. No code changes in this task.</action>
  <verify>Manual visual inspection per the how-to-verify steps below.</verify>
  <done>User confirms settings panel looks correct, persists values, and sends config to backend.</done>
  <what-built>Collapsible rate limit settings panel in the simulation view (Step 3). Settings persist to localStorage and are sent to backend before simulation starts.</what-built>
  <how-to-verify>
    1. Navigate to a simulation in the app (reach Step 3 — the simulation run view)
    2. Before starting the simulation, you should see a "Rate Limit Settings" toggle bar
    3. Click it to expand — verify 5 controls appear:
       - Inter-turn Delay slider (0-5000ms, shows current value)
       - Max Retries number input (default 3)
       - Retry Base Delay number input (default 30)
       - TPM Limit number input (default 0)
       - RPM Limit number input (default 0)
    4. Adjust some values, refresh the page — values should persist (localStorage)
    5. Start a simulation — the settings panel should disappear once running
    6. Check browser console network tab — a POST to /api/simulation/{id}/config should fire before the /start call
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Settings panel renders in Step3Simulation.vue when phase === 0
2. All 5 controls match D-08 spec (slider for delay, number inputs for retries/delay/TPM/RPM)
3. localStorage persistence works across page reloads
4. POST /api/simulation/{id}/config fires before simulation start
5. Panel hides when simulation is running (phase !== 0)
6. Styles use existing CSS custom properties (--card, --border, --primary, etc.)
</verification>

<success_criteria>
- Rate limit settings panel visible and functional in pre-run state
- All 5 settings controllable with correct ranges and defaults
- Settings survive page refresh via localStorage
- Settings transmitted to backend before simulation launch
- Panel hidden during and after simulation run
- Visual styling consistent with existing Slater Consulting dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-rate-limit-control/03-02-SUMMARY.md`
</output>
