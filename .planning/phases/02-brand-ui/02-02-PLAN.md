---
phase: 02-brand-ui
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/views/Home.vue
  - frontend/src/views/MainView.vue
  - frontend/src/views/SimulationView.vue
  - frontend/src/views/SimulationRunView.vue
  - frontend/src/views/ReportView.vue
  - frontend/src/components/Step1GraphBuild.vue
  - frontend/src/components/Step2EnvSetup.vue
  - frontend/src/components/Step3Simulation.vue
  - frontend/src/components/Step4Report.vue
  - frontend/src/components/Step5Interaction.vue
  - frontend/src/components/HistoryDatabase.vue
  - frontend/src/components/GraphPanel.vue
autonomous: false
requirements: [R2.3, R2.4]

must_haves:
  truths:
    - "All 5 header locations show Slater Consulting instead of MIROFISH"
    - "Buttons use --primary background with --primary-foreground text"
    - "Cards use --secondary or --card background with --border borders"
    - "No hardcoded orange (#FF5722, #FF4500) remains in any .vue file"
    - "No hardcoded green success (#10B981, #4CAF50, #1A936F, #2E7D32) remains -- replaced with --accent"
    - "No hardcoded white (#FFFFFF, #FFF, #FAFAFA) backgrounds remain -- replaced with token vars"
    - "D3 graph colors use dark-theme-aware palette readable on dark navy background"
    - "All components visually consistent with Slater Consulting dark navy/blue theme"
  artifacts:
    - path: "frontend/src/views/MainView.vue"
      provides: "Rebranded header + tokenized status colors"
      contains: "Slater Consulting"
    - path: "frontend/src/components/GraphPanel.vue"
      provides: "Dark-theme D3 color palette"
      contains: "#4A9EFF"
  key_links:
    - from: "frontend/src/views/MainView.vue"
      to: "frontend/src/style.css"
      via: "var(--primary) references"
      pattern: "var\\(--primary\\)"
    - from: "frontend/src/components/Step1GraphBuild.vue"
      to: "frontend/src/style.css"
      via: "var(--secondary) for card backgrounds"
      pattern: "var\\(--secondary\\)"
    - from: "frontend/src/components/Step4Report.vue"
      to: "frontend/src/style.css"
      via: "--wf-* vars remapped to global tokens"
      pattern: "var\\(--border\\)"
---

<objective>
Audit and rebrand all 12 Vue component files: replace hardcoded hex colors with CSS custom property tokens, update header brand text to "Slater Consulting", and replace D3 graph palette for dark theme.

Purpose: This is where the visual transformation happens. Plan 01 established the tokens; this plan applies them to every component so the app looks like a Slater Consulting product.

Output: All 12 files updated with token-based colors. All headers show "Slater Consulting". D3 graph uses dark-theme palette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-ui/02-CONTEXT.md
@.planning/phases/02-brand-ui/02-UI-SPEC.md
@.planning/phases/02-brand-ui/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand views -- header text + color token audit for all 5 view files</name>
  <files>
    frontend/src/views/Home.vue,
    frontend/src/views/MainView.vue,
    frontend/src/views/SimulationView.vue,
    frontend/src/views/SimulationRunView.vue,
    frontend/src/views/ReportView.vue
  </files>
  <read_first>
    frontend/src/views/Home.vue,
    frontend/src/views/MainView.vue,
    frontend/src/views/SimulationView.vue,
    frontend/src/views/SimulationRunView.vue,
    frontend/src/views/ReportView.vue,
    .planning/phases/02-brand-ui/02-UI-SPEC.md
  </read_first>
  <action>
**A. Header brand text (per D-09):**

In each of the 4 main views, find the `.brand` div and change its text:
- `MainView.vue` line ~6: `<div class="brand" ...>MIROFISH</div>` -> `<div class="brand" ...>Slater Consulting</div>`
- `SimulationView.vue` line ~6: same change
- `SimulationRunView.vue` line ~6: same change
- `ReportView.vue` line ~6: same change

In `Home.vue`, find `.nav-brand` and change:
- `<div class="nav-brand">MIROFISH</div>` -> `<div class="nav-brand">Slater Consulting</div>`

**B. Color substitution for all 5 view files:**

Apply the UI-SPEC Color Substitution Map to every `<style scoped>` block. Use find-and-replace for each file:

| Find (in CSS values) | Replace With | Notes |
|---|---|---|
| `#000000` or `#000` (in color/background context) | `var(--foreground)` | Text on dark bg |
| `#FFFFFF` or `#FFF` (background) | `var(--background)` | Page backgrounds |
| `#FFFFFF` or `#FFF` (text color on dark element) | `var(--primary-foreground)` | Text on buttons |
| `#FAFAFA` or `#F8F9FA` (card/panel bg) | `var(--secondary)` | Card surfaces |
| `#EAEAEA` or `#E5E7EB` (borders) | `var(--border)` | Borders, dividers |
| `#FF5722` (orange active/processing) | `var(--primary)` | Active states |
| `#4CAF50` or `#1A936F` (green completed) | `var(--accent)` | Success/completed per D-04 |
| `#E8F5E9` (green badge bg) | `var(--secondary)` | Success badge bg |
| `#2E7D32` (green badge text) | `var(--accent)` | Success badge text |
| `#F44336` (error red) | `var(--destructive)` | Error states |
| `#6B7280` or `#9CA3AF` (muted text) | `var(--muted-foreground)` | Secondary text |
| `rgba(0,0,0,0.15)` or `rgba(0,0,0,0.25)` (overlays) | `rgba(0,0,0,0.4)` | Dark overlays on dark bg |

**Home.vue additional changes:**
- All remaining hardcoded hex colors in `<style scoped>` that were NOT part of the `:root` block (which was already deleted in Plan 01 Task 2) must be replaced per the substitution map above.
- `font-family: 'Space Grotesk'` -> remove or replace with `font-family: 'Geist Sans', 'Inter', system-ui, sans-serif`
- `font-family: 'Noto Sans SC'` -> remove entirely

**For all 4 non-Home views (MainView, SimulationView, SimulationRunView, ReportView):**
- These share the same header/status pattern. Each has `.brand` styling, status indicator colors, and step card borders.
- `.brand` style: update `color` to `var(--foreground)`, `background` references to `var(--background)` or `var(--secondary)`.
- `.header` or `.top-bar`: background to `var(--secondary)`, border to `var(--border)`.
- `.badge.processing` or `.status-indicator.processing .dot`: background `var(--primary)`.
- `.badge.completed` or equivalent: background `var(--accent)`.
- `.badge.error`: background `var(--destructive)`.
- `.step-card`: background `var(--card)`, border `var(--border)`.
- `.step-card.active`: border-color `var(--primary)`.
- `.action-btn` or primary buttons: background `var(--primary)`, color `var(--primary-foreground)`.
- Any `font-family: 'Space Grotesk'` -> remove the declaration (inherits Geist Sans from #app).
  </action>
  <verify>
    <automated>cd "c:/Users/lucas/OneDrive/Documentos/AI/VS Code/MiroFish SIPE/mirofish/frontend" && grep -l "Slater Consulting" src/views/Home.vue src/views/MainView.vue src/views/SimulationView.vue src/views/SimulationRunView.vue src/views/ReportView.vue | wc -l | grep -q "5" && ! grep -rl "#FF5722\|#4CAF50\|#1A936F" src/views/ && echo "VIEWS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 view files contain "Slater Consulting" (grep confirms 5 matches)
    - No view file contains "MIROFISH" as brand text (template section)
    - No view file contains `#FF5722` (orange)
    - No view file contains `#4CAF50` or `#1A936F` (green success)
    - No view file contains `#F44336` as a raw hex (should be `var(--destructive)`)
    - `MainView.vue` contains `var(--primary)` (at least one usage)
    - `MainView.vue` contains `var(--secondary)` or `var(--card)` (at least one usage)
    - `Home.vue` does not contain `font-family: 'Space Grotesk'`
    - `Home.vue` does not contain `font-family: 'Noto Sans SC'`
    - `npm run build` in `frontend/` exits with code 0
  </acceptance_criteria>
  <done>All 5 view files rebranded: headers say "Slater Consulting", all hardcoded hex colors replaced with CSS custom property tokens, deprecated font-family declarations removed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Color token audit for all 7 component files including D3 palette</name>
  <files>
    frontend/src/components/Step1GraphBuild.vue,
    frontend/src/components/Step2EnvSetup.vue,
    frontend/src/components/Step3Simulation.vue,
    frontend/src/components/Step4Report.vue,
    frontend/src/components/Step5Interaction.vue,
    frontend/src/components/HistoryDatabase.vue,
    frontend/src/components/GraphPanel.vue
  </files>
  <read_first>
    frontend/src/components/Step1GraphBuild.vue,
    frontend/src/components/Step2EnvSetup.vue,
    frontend/src/components/Step3Simulation.vue,
    frontend/src/components/Step4Report.vue,
    frontend/src/components/Step5Interaction.vue,
    frontend/src/components/HistoryDatabase.vue,
    frontend/src/components/GraphPanel.vue,
    .planning/phases/02-brand-ui/02-UI-SPEC.md
  </read_first>
  <action>
Apply the UI-SPEC Color Substitution Map to all 7 component files. Work through each file systematically.

**SUBSTITUTION MAP (same as Task 1, apply to `<style scoped>` blocks AND template inline styles/SVG attributes):**

| Find | Replace With |
|---|---|
| `#000000`, `#000`, `#111827`, `#1F2937` (text) | `var(--foreground)` |
| `#FFFFFF`, `#FFF`, `#F8F9FA`, `#FAFAFA`, `#F9FAFB`, `#FAFBFC` (backgrounds) | `var(--background)` for page bg, `var(--secondary)` for card bg |
| `#EAEAEA`, `#E5E7EB`, `#F3F4F6`, `#E8EAED`, `#D1D5DB`, `#e8eaed`, `#e5e7eb` (borders) | `var(--border)` |
| `#FF5722`, `#FF4500` (orange) | `var(--primary)` |
| `#10B981`, `#1A936F`, `#4CAF50`, `#2E7D32`, `#047857`, `#065F46` (green) | `var(--accent)` per D-04 |
| `#ECFDF5`, `#D1FAE5`, `#E8F5E9` (green bg) | `var(--secondary)` |
| `#F44336`, `#C5283D` (error red) | `var(--destructive)` |
| `#6B7280`, `#9CA3AF`, `#4B5563`, `#374151` (muted gray) | `var(--muted-foreground)` |
| `#3498db`, `#3B82F6`, `#2563EB` (existing blues) | `var(--accent)` |
| `#F59E0B` (amber) | `var(--accent)` |
| `rgba(0,0,0,0.15)` or similar light overlays | `rgba(0,0,0,0.4)` |

**INLINE SVG STROKES (in `<template>` sections, not just `<style>`):**
- `stroke="#E5E7EB"` -> `stroke="var(--border)"` (spinner track)
- `stroke="#4B5563"` -> `stroke="var(--accent)"` (spinner head)
Search template sections for `stroke="#` and `fill="#` patterns.

**PER-COMPONENT SPECIFICS:**

**Step1GraphBuild.vue:**
- `.step-card`: background `var(--card)`, border `var(--border)`
- `.action-btn`: background `var(--primary)`, color `var(--primary-foreground)`
- `.badge.completed` / success states: background `var(--secondary)`, color/border `var(--accent)`
- `.badge.processing`: background `var(--primary)`
- Log area background: `var(--secondary)`

**Step2EnvSetup.vue:**
- `.step-card.active` border: `var(--primary)`
- Spinner/processing: `var(--primary)`
- Completed state green: `var(--accent)`
- Button `#000` bg: `var(--primary)`
- Button `#FFF` text: `var(--primary-foreground)`

**Step3Simulation.vue:**
- Completed state `#1A936F`: `var(--accent)`
- Button backgrounds `#000`: `var(--primary)`
- Button text `#FFF`: `var(--primary-foreground)`
- Borders `#EAEAEA`: `var(--border)`

**Step4Report.vue:**
- Remap ALL `--wf-*` local CSS variables:
  - `--wf-border: #E5E7EB` -> `--wf-border: var(--border)`
  - `--wf-active-bg` (light blue) -> `--wf-active-bg: var(--secondary)`
  - `--wf-active-border` (blue) -> `--wf-active-border: var(--primary)`
  - `--wf-active-dot` (blue) -> `--wf-active-dot: var(--primary)`
  - `--wf-done-bg` (green-tinted) -> `--wf-done-bg: var(--card)`
  - Any `#10B981` / `#1F2937` in local vars -> `var(--accent)` / `var(--foreground)`
- Inline SVG strokes in template: apply stroke substitutions above
- Tool color strings in JS (if any like 'purple', 'blue'): leave as-is unless they are hex values

**Step5Interaction.vue (LARGEST -- 50+ hardcoded values per UI-SPEC):**
- This component is extensively white-themed. Apply the full substitution map.
- Chat message bubbles: user messages `var(--secondary)`, system messages `var(--card)`
- Green chat badges `#ECFDF5`/`#047857` -> `var(--secondary)`/`var(--accent)`
- All inline SVG strokes in template section
- `#F8F9FA`, `#FFFFFF` backgrounds -> `var(--secondary)` or `var(--background)`
- `#000000`, `#111827` text -> `var(--foreground)`
- `#9CA3AF` muted text -> `var(--muted-foreground)`

**HistoryDatabase.vue:**
- White card backgrounds `#FFFFFF`, `#F3F4F6` -> `var(--card)`, `var(--secondary)`
- Border `#E5E7EB` -> `var(--border)`
- Status icon colors: `#3B82F6` (blue) -> `var(--accent)`, `#F59E0B` (amber) -> `var(--accent)`, `#10B981` (green) -> `var(--accent)`
- File tag colors -> use `var(--secondary)` bg with `var(--foreground)` text

**GraphPanel.vue (D3 -- per D-12):**
This file uses D3 inline JS, NOT CSS. Do NOT use CSS var() for D3 attributes.
- Replace the 10-color palette array:
  FROM: `['#FF6B35', '#004E89', '#7B2D8E', '#1A936F', '#C5283D', '#E9724C', '#3498db', '#9b59b6', '#27ae60', '#f39c12']`
  TO: `['#4A9EFF', '#7C5CFC', '#2DD4BF', '#F472B6', '#FB923C', '#A78BFA', '#34D399', '#60A5FA', '#C084FC', '#FBBF24']`
- Replace D3 stroke/fill values in JS:
  - `"#C0C0C0"` (link/edge) -> `"#3A5575"`
  - `rgba(255,255,255,0.95)` (label bg) -> `hsl(213, 50%, 14%)` (--card value)
  - `"#666"` (label text) -> `"hsl(213, 20%, 60%)"` (--muted-foreground value)
  - `rgba(255,255,255,0.9)` (hover label bg) -> `hsl(213, 40%, 18%)` (--secondary value)
  - `"#3498db"` (selected link) -> `"hsl(213, 75%, 60%)"` (--accent value)
  - `"#E91E63"` (selected node) -> `"hsl(213, 75%, 60%)"` (--accent value)
  - `"#fff"` (node stroke) -> `"hsl(210, 57%, 11%)"` (--background value)
  - `"#333"` (node label text) -> `"hsl(220, 33%, 97%)"` (--foreground value)
- Also update any CSS in `<style scoped>` using the standard substitution map
  </action>
  <verify>
    <automated>cd "c:/Users/lucas/OneDrive/Documentos/AI/VS Code/MiroFish SIPE/mirofish/frontend" && ! grep -rl "#FF5722\|#FF4500\|#4CAF50\|#1A936F\|#10B981\|#2E7D32" src/components/ && grep -q "#4A9EFF" src/components/GraphPanel.vue && grep -q "var(--primary)" src/components/Step1GraphBuild.vue && grep -q "var(--accent)" src/components/Step2EnvSetup.vue && echo "COMPONENTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - No component file contains `#FF5722` or `#FF4500` (orange)
    - No component file contains `#4CAF50`, `#1A936F`, `#10B981`, `#2E7D32`, `#047857` (green success)
    - No component file contains `#E8F5E9` or `#ECFDF5` (green backgrounds)
    - `GraphPanel.vue` contains `#4A9EFF` (first color in new D3 palette)
    - `GraphPanel.vue` does NOT contain `#FF6B35` (old palette first color)
    - `GraphPanel.vue` contains `hsl(220, 33%, 97%)` for node label text
    - `Step1GraphBuild.vue` contains `var(--primary)`
    - `Step2EnvSetup.vue` contains `var(--accent)`
    - `Step4Report.vue` `--wf-border` value is `var(--border)` (not `#E5E7EB`)
    - `Step5Interaction.vue` contains `var(--foreground)` (text color)
    - `Step5Interaction.vue` does NOT contain `#F8F9FA` or `#FFFFFF` as CSS values
    - `HistoryDatabase.vue` contains `var(--card)` or `var(--secondary)`
    - `HistoryDatabase.vue` does NOT contain `#F59E0B` (amber)
    - `npm run build` in `frontend/` exits with code 0
  </acceptance_criteria>
  <done>All 7 component files audited and tokenized. No hardcoded orange, green, or white-theme colors remain. D3 palette replaced with dark-theme-aware colors. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Slater Consulting brand UI</name>
  <files>none</files>
  <action>
Complete Slater Consulting brand UI has been applied to all Vue components. Dark navy/blue theme with Geist Sans font, updated headers, tokenized colors across all 12 files, D3 graph with dark-theme palette. Human must visually verify the result.

Steps for verification:
1. Start the frontend dev server: `cd frontend && npm run dev`
2. Open http://localhost:5173 in the browser
3. Verify Home page:
   - Dark navy background (not white)
   - "Slater Consulting" in navbar (not "MIROFISH")
   - MiroFish logo image still present in hero section
   - Orange tags/buttons replaced with blue
   - Geist Sans font (check browser devtools Computed > font-family)
4. Click into a simulation to verify MainView:
   - Header shows "Slater Consulting"
   - Step cards have dark background with blue borders
   - Processing badges are blue (not orange)
   - Completed badges are lighter blue (not green)
5. Check the Graph panel:
   - Node colors are blues/purples/teals (not orange/green)
   - Node labels are readable (light text on dark)
   - Link/edge colors are visible on dark background
6. Check the Report view and Step5 interaction panel:
   - Dark themed (no white panels remaining)
   - Text is readable (light on dark)
7. Check the browser tab:
   - Favicon shows "SC" on dark navy (not orange MiroFish icon)
   - Tab title: "MiroFish SIPE -- Slater Consulting"
  </action>
  <verify>Human visual inspection of running application</verify>
  <done>User confirms the app visually matches the Slater Consulting dark navy/blue theme across all pages. Type "approved" or describe any visual issues that need fixing.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` exits 0
2. `grep -rl "#FF5722" frontend/src/` returns NO matches
3. `grep -rl "#4CAF50\|#1A936F\|#10B981" frontend/src/` returns NO matches
4. `grep -rl "MIROFISH" frontend/src/views/` returns NO matches (brand text replaced)
5. `grep -rl "Slater Consulting" frontend/src/views/ | wc -l` returns 5
6. `grep -c "var(--" frontend/src/components/Step1GraphBuild.vue` returns 5+ (tokens in use)
7. Visual walkthrough confirms dark theme across all pages
</verification>

<success_criteria>
- All 5 header locations display "Slater Consulting"
- All hardcoded orange (#FF5722, #FF4500) removed from entire frontend/src/
- All hardcoded green success (#10B981, #4CAF50, #1A936F, #2E7D32) replaced with --accent
- D3 graph palette updated and readable on dark background
- All component backgrounds use --background, --secondary, --card, or --border tokens
- Build passes, visual inspection approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-ui/02-02-SUMMARY.md`
</output>
