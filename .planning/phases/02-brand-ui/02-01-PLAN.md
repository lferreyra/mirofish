---
phase: 02-brand-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/style.css
  - frontend/src/main.js
  - frontend/index.html
  - frontend/src/App.vue
  - frontend/public/favicon.svg
  - frontend/src/views/Home.vue
autonomous: true
requirements: [R2.1, R2.2, R2.3]

must_haves:
  truths:
    - "CSS custom properties with Slater Consulting palette are globally available to all components"
    - "Geist Sans font loads and renders as the default body font"
    - "Page title reads MiroFish SIPE — Slater Consulting"
    - "Favicon shows SC initials on dark navy background"
    - "No conflicting :root block remains in Home.vue"
    - "Google Fonts CDN links for Inter, Space Grotesk, Noto Sans SC are removed"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "Global CSS custom property token system"
      contains: "--background: hsl(210, 57%, 11%)"
    - path: "frontend/public/favicon.svg"
      provides: "Slater Consulting SC favicon"
      contains: "<svg"
  key_links:
    - from: "frontend/src/main.js"
      to: "frontend/src/style.css"
      via: "import './style.css'"
      pattern: "import.*style\\.css"
    - from: "frontend/src/main.js"
      to: "@fontsource/geist-sans"
      via: "import '@fontsource/geist-sans/400.css'"
      pattern: "fontsource/geist-sans"
    - from: "frontend/index.html"
      to: "frontend/public/favicon.svg"
      via: "link rel=icon href=/favicon.svg"
      pattern: "favicon\\.svg"
---

<objective>
Establish the Slater Consulting brand token system: CSS custom properties, Geist Sans font, updated index.html metadata, favicon, and App.vue global styles.

Purpose: Creates the foundation that all component-level color replacements (Plan 02) depend on. After this plan, `var(--primary)` etc. are available globally but no component yet uses them (expected -- visual changes come in Plan 02).

Output: `style.css` token file, updated `main.js` imports, cleaned `index.html`, `favicon.svg`, updated `App.vue` global styles, Home.vue `:root` conflict resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-ui/02-CONTEXT.md
@.planning/phases/02-brand-ui/02-UI-SPEC.md

<interfaces>
<!-- Current state of files being modified -->

From frontend/src/main.js (entire file):
```js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(router)
app.mount('#app')
```

From frontend/index.html (entire file):
```html
<!doctype html>
<html lang="en">
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@100..800&family=Noto+Sans+SC:wght@300;400;500;700;800;900&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">
    <meta charset="UTF-8" />
    <link rel="icon" type="image/png" href="/icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="MiroFish - Swarm Intelligence Prediction Engine" />
    <title>MiroFish - Predict Anything</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

From frontend/src/App.vue <style> block:
```css
/* Global style reset */
* { margin: 0; padding: 0; box-sizing: border-box; }

#app {
  font-family: 'JetBrains Mono', 'Space Grotesk', 'Noto Sans SC', monospace;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  color: #000000;
  background-color: #ffffff;
}

::-webkit-scrollbar { width: 8px; height: 8px; }
::-webkit-scrollbar-track { background: #f1f1f1; }
::-webkit-scrollbar-thumb { background: #000000; }
::-webkit-scrollbar-thumb:hover { background: #333333; }

button { font-family: inherit; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token system, install font, update entry points</name>
  <files>
    frontend/src/style.css,
    frontend/src/main.js,
    frontend/index.html,
    frontend/public/favicon.svg,
    frontend/src/App.vue
  </files>
  <read_first>
    frontend/src/main.js,
    frontend/index.html,
    frontend/src/App.vue,
    .planning/phases/02-brand-ui/02-UI-SPEC.md
  </read_first>
  <action>
1. Install Geist Sans font package (per D-06):
   ```bash
   cd frontend && npm install @fontsource/geist-sans
   ```

2. Create `frontend/src/style.css` with EXACTLY this content (per D-03, R2.1):
   ```css
   /* Slater Consulting brand tokens — Phase 2 */

   :root {
     --background:             hsl(210, 57%, 11%);
     --foreground:             hsl(220, 33%, 97%);
     --primary:                hsl(217, 75%, 47%);
     --primary-foreground:     hsl(0, 0%, 100%);
     --secondary:              hsl(213, 40%, 18%);
     --secondary-foreground:   hsl(220, 33%, 97%);
     --accent:                 hsl(213, 75%, 60%);
     --accent-foreground:      hsl(0, 0%, 100%);
     --muted-foreground:       hsl(213, 20%, 60%);
     --border:                 hsl(213, 30%, 22%);
     --card:                   hsl(213, 50%, 14%);
     --destructive:            hsl(0, 84%, 60%);
     --destructive-foreground: hsl(0, 0%, 100%);
   }
   ```
   Do NOT include `* {}` reset or `body {}` in style.css -- those stay in App.vue's global `<style>`.

3. Update `frontend/src/main.js` -- add imports BEFORE the `createApp` line, in this order:
   ```js
   import './style.css'
   import '@fontsource/geist-sans/400.css'
   import '@fontsource/geist-sans/600.css'
   ```
   Keep all existing imports (`createApp`, `App`, `router`) and app setup unchanged.

4. Update `frontend/index.html`:
   a. Replace the SINGLE Google Fonts `<link href="...">` line (line 6) with a JetBrains-Mono-only link:
      ```html
      <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@100..800&display=swap" rel="stylesheet">
      ```
      This removes Inter, Space Grotesk, and Noto Sans SC (per D-08). Keep both `<link rel="preconnect">` lines.
   b. Replace the favicon line:
      FROM: `<link rel="icon" type="image/png" href="/icon.png" />`
      TO: `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` (per D-11)
   c. Replace the meta description:
      FROM: `content="MiroFish - Swarm Intelligence Prediction Engine"`
      TO: `content="MiroFish SIPE — Slater Consulting Prediction Platform"` (per D-10, note the em dash)
   d. Replace the title:
      FROM: `<title>MiroFish - Predict Anything</title>`
      TO: `<title>MiroFish SIPE — Slater Consulting</title>` (per D-10, note the em dash)

5. Create `frontend/public/favicon.svg` with EXACTLY this content (per D-11):
   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
     <rect width="32" height="32" rx="6" fill="hsl(210, 57%, 11%)"/>
     <text x="16" y="22" font-family="Geist Sans, Inter, system-ui" font-size="14" font-weight="600"
       fill="hsl(220, 33%, 97%)" text-anchor="middle">SC</text>
   </svg>
   ```

6. Update `frontend/src/App.vue` `<style>` block (global, NOT scoped). Replace the ENTIRE `<style>` block content with:
   ```css
   /* Global style reset */
   * {
     margin: 0;
     padding: 0;
     box-sizing: border-box;
   }

   #app {
     font-family: 'Geist Sans', 'Inter', system-ui, sans-serif;
     -webkit-font-smoothing: antialiased;
     -moz-osx-font-smoothing: grayscale;
     color: var(--foreground);
     background-color: var(--background);
   }

   /* Scrollbar styles */
   ::-webkit-scrollbar {
     width: 8px;
     height: 8px;
   }

   ::-webkit-scrollbar-track {
     background: var(--border);
   }

   ::-webkit-scrollbar-thumb {
     background: var(--muted-foreground);
   }

   ::-webkit-scrollbar-thumb:hover {
     background: var(--accent);
   }

   /* Global button styles */
   button {
     font-family: inherit;
   }
   ```
   The `<template>` and `<script setup>` sections remain unchanged.
  </action>
  <verify>
    <automated>cd "c:/Users/lucas/OneDrive/Documentos/AI/VS Code/MiroFish SIPE/mirofish/frontend" && grep -c "geist-sans" package.json && grep "import.*style\.css" src/main.js && grep "fontsource/geist-sans" src/main.js && grep "\-\-background.*hsl(210" src/style.css && grep "var(--foreground)" src/App.vue && grep "favicon.svg" index.html && grep "Slater Consulting" index.html && test -f public/favicon.svg && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/style.css` exists and contains `--background: hsl(210, 57%, 11%)`
    - `frontend/src/style.css` contains all 13 tokens: --background, --foreground, --primary, --primary-foreground, --secondary, --secondary-foreground, --accent, --accent-foreground, --muted-foreground, --border, --card, --destructive, --destructive-foreground
    - `frontend/src/main.js` contains `import './style.css'` BEFORE `import { createApp }`
    - `frontend/src/main.js` contains `import '@fontsource/geist-sans/400.css'`
    - `frontend/src/main.js` contains `import '@fontsource/geist-sans/600.css'`
    - `frontend/package.json` contains `@fontsource/geist-sans` in dependencies
    - `frontend/index.html` does NOT contain `fonts.googleapis.com/css2?family=Inter`
    - `frontend/index.html` does NOT contain `Space+Grotesk`
    - `frontend/index.html` does NOT contain `Noto+Sans+SC`
    - `frontend/index.html` DOES contain `JetBrains+Mono`
    - `frontend/index.html` contains `<title>MiroFish SIPE — Slater Consulting</title>`
    - `frontend/index.html` contains `href="/favicon.svg"`
    - `frontend/index.html` contains `Slater Consulting Prediction Platform`
    - `frontend/public/favicon.svg` exists and contains `<svg` and `SC</text>`
    - `frontend/src/App.vue` contains `font-family: 'Geist Sans', 'Inter', system-ui, sans-serif`
    - `frontend/src/App.vue` contains `color: var(--foreground)`
    - `frontend/src/App.vue` contains `background-color: var(--background)`
    - `frontend/src/App.vue` does NOT contain `#000000` or `#ffffff`
    - `frontend/src/App.vue` scrollbar uses `var(--border)` and `var(--muted-foreground)`
  </acceptance_criteria>
  <done>Token system file created with all 13 CSS custom properties. Geist Sans installed and imported. index.html cleaned (CDN fonts trimmed to JetBrains Mono only, title/meta/favicon updated). App.vue uses token vars for all colors. favicon.svg created.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Home.vue :root block and replace orange var references</name>
  <files>frontend/src/views/Home.vue</files>
  <read_first>
    frontend/src/views/Home.vue,
    .planning/phases/02-brand-ui/02-UI-SPEC.md
  </read_first>
  <action>
Per D-05: Delete the ENTIRE `:root { }` block from `Home.vue`'s `<style scoped>` section. This block currently defines `--black`, `--white`, `--orange`, `--border`, `--font-mono`, `--font-sans` and conflicts with the global token system.

Then find ALL instances of `var(--orange)` in Home.vue and replace:
- `var(--orange)` used for primary interactive elements (buttons, tags) -> `var(--primary)`
- `var(--orange)` used for hover states -> `var(--accent)`

Specific expected replacements (search for each):
- `.orange-tag { background: var(--orange); }` -> `background: var(--primary);`
- `.start-engine-btn:hover { background: var(--orange); }` -> `background: var(--accent);`
- Any other `var(--orange)` -> `var(--primary)` (default) or `var(--accent)` if it's a hover/highlight

Also replace `var(--black)` -> `var(--foreground)`, `var(--white)` -> `var(--background)`, and `var(--border)` references that pointed to `#E5E5E5` -> they now correctly inherit from the global `--border` token (no change needed for the var reference itself, just verify).

Do NOT change the MiroFish logo image or hero section copy (per D-01, D-02).
  </action>
  <verify>
    <automated>cd "c:/Users/lucas/OneDrive/Documentos/AI/VS Code/MiroFish SIPE/mirofish/frontend" && ! grep -q "\-\-orange" src/views/Home.vue && ! grep -q "\-\-black" src/views/Home.vue && ! grep -q "\-\-white:" src/views/Home.vue && ! grep -q "#FF4500" src/views/Home.vue && echo "HOME VUE CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/views/Home.vue` does NOT contain `--orange`
    - `frontend/src/views/Home.vue` does NOT contain `--black:` or `var(--black)`
    - `frontend/src/views/Home.vue` does NOT contain `--white:` (as a custom property definition)
    - `frontend/src/views/Home.vue` does NOT contain `#FF4500`
    - `frontend/src/views/Home.vue` does NOT contain a `:root {` block in its `<style scoped>` section
    - `frontend/src/views/Home.vue` DOES still contain `MiroFish_logo_left.jpeg` (logo preserved per D-01)
    - `npm run build` in `frontend/` exits with code 0
  </acceptance_criteria>
  <done>Home.vue :root conflict block deleted. All `var(--orange)` replaced with `var(--primary)` or `var(--accent)`. No conflicting custom property definitions remain. Build succeeds.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd frontend && npm run build` exits 0 (no broken imports or syntax)
2. `grep -r "var(--background)" frontend/src/App.vue` returns a match
3. `grep -r "var(--primary)" frontend/src/style.css` returns a match
4. `grep -c "fontsource" frontend/src/main.js` returns 2
5. No `:root` block in Home.vue scoped styles
6. `frontend/public/favicon.svg` exists
</verification>

<success_criteria>
- All 13 CSS custom property tokens defined in style.css and globally available
- Geist Sans font installed via @fontsource and imported in main.js
- index.html: title says "MiroFish SIPE -- Slater Consulting", CDN trimmed to JetBrains Mono only, favicon points to SVG
- App.vue: uses var() tokens for all colors, Geist Sans as primary font
- Home.vue: :root block deleted, no --orange/--black/--white custom props remain
- Build passes (`npm run build` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-ui/02-01-SUMMARY.md`
</output>
