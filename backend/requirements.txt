# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM Providers =============
# OpenAI SDK (for OpenAI and compatible APIs)
openai>=1.0.0
# Anthropic SDK (for Claude models)
anthropic>=0.40.0

# ============= Graph Database =============
# KuzuDB - embedded local graph database
kuzu>=0.8.0

# ============= OASIS Social Simulation =============
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utilities =============
# Environment variable loading
python-dotenv>=1.0.0
# WSGI server
waitress>=3.0.0
# Data validation
pydantic>=2.0.0
