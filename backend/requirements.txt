# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= 核心框架 =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM 相关 =============
# OpenAI SDK（统一使用 OpenAI 格式调用 LLM）
openai>=1.0.0

# ============= Zep Cloud =============
zep-cloud==3.13.0

# ============= OASIS 社交媒体模拟 =============
# OASIS 社交模拟框架
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= 文件处理 =============
PyMuPDF>=1.24.0
# 编码检测（支持非UTF-8编码的文本文件）
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= 工具库 =============
# 环境变量加载
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0

# ============= Phase 3: transport + streaming + checkpoint =============
# ZeroMQ for command / event transport
pyzmq>=26.0.0
# WebSocket support for live event fan-out + streaming interviews
flask-sock>=0.7.0
# Fast compression for checkpoint archives (optional — gzip fallback is used when missing)
zstandard>=0.22.0

# ============= Phase 2: Neo4j backend (optional) =============
# Enable by setting MEMORY_BACKEND=neo4j_local or neo4j_aura
neo4j>=5.0.0
